/** Shared auth field sizing — colors come from Input / Button semantic tokens. */
export const authFieldClass =
  "h-12 rounded-full border-border bg-popover px-5 text-base shadow-sm placeholder:text-muted-foreground md:text-sm"

/** SelectTrigger overrides — default shadcn select is h-8 via data-[size=default]. */
export const authSelectTriggerClass =
  "h-12 w-full rounded-full border-border bg-popover px-5 py-0 text-base shadow-sm data-[size=default]:h-12 data-placeholder:text-muted-foreground md:text-sm [&_svg]:size-5"

export const authButtonClass = "h-12 w-full rounded-full"
